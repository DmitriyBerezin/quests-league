extends ../layout

block style
	link(rel='stylesheet', href='/stylesheets/style.css')
	link(rel='stylesheet', href='/stylesheets/admin/quest.css')


block content
	h1.page-header Список комментариев для модерирования

	if comments.length > 0
		ul.list-group
			each comment in comments
				li.list-group-item
					a(href="/admin/comment/approve/#{comment.comment_id}") Посмотреть отзыв
					a(href="/quest/#{comment.quest_id}").pull-right= comment.quest_name
	else
		p Отдохните! Все квесты проверены модераторами.
